import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { MapownPage } from './mapown';

@NgModule({
  declarations: [
    MapownPage,
  ],
  imports: [
    IonicPageModule.forChild(MapownPage),
  ],
})
export class MapownPageModule {}
